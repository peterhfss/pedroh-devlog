export type TabItem = {
    title: string;
    pathName: string;
    
}

export type ProjectType = {
    name: string;
    desc: string;
    lang: string;
    langColor: string;
    url: string;

}

export type TagType = {
    label: string;
    color:string;
}
